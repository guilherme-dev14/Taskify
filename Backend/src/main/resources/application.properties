spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:false}
jwt.secret=${JWT_SECRET:dGFza2lmeS1zcHJpbmctYm9vdC1qd3Qtc2VjcmV0LWtleS12ZXJ5LXNlY3VyZS1hbmQtbG9uZw==}
jwt.expiration=${JWT_EXPIRATION:86400}

spring.security.debug=false

logging.level.com.taskifyApplication=${LOG_LEVEL:INFO}
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=${HIBERNATE_LOG_LEVEL:INFO}

server.port=8080


# --- Google Cloud Storage Settings ---
spring.cloud.gcp.credentials.location=classpath:GoogleStorageJson.json

spring.cloud.gcp.project-id=splendid-flow-473618-k8

gcs.bucket.name=taskify-attachments

spring.application.name=Taskify

spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=9841cd001@smtp-brevo.com
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=support@taskify.life
app.frontendBaseUrl=${FRONTEND_URL}
app.resetTokenTtlMinutes=30
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# File upload configuration
app.upload.dir=uploads
app.upload.max-file-size=50MB
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB